# Server-side: where the Next.js /api/copilotkit route forwards AG-UI requests.
# In Docker compose this points to the backend service; for local dev outside docker,
# use http://localhost:8000/agent.
AG_UI_BACKEND_URL=http://localhost:8000/agent
